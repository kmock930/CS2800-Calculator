/**
 * 
 * @author Kelvin
 * 
 *         This is the generic interface for a binary operator that evaluates a
 *         pair of integers.
 *
 */
public interface Operator {
  float eval(float first, float second);
}
